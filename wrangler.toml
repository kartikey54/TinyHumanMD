name = "tinyhumanmd"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."

# Pages doesn't use [build] or [env] sections like Workers
# It uses project-level settings. We just need the name and output dir.
